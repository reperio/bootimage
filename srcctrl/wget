##
# Get a known-good copy of:
# wget
# 

. ./functions

FILENAME='wget-1.11.3.tar.bz2'
URL='http://ftp.gnu.org/gnu/wget/wget-1.11.3.tar.bz2'
DIRNAME='wget-1.11.3'

BINARIES="\
wget"

#build () {
#  cd ${SRCDIR}/${DIRNAME}
#  ./autogen.sh ${CONFIGOPTIONS}
#  make
#  make anothertask
#}

#install () {
#  cp -pv ${SRCDIR}/${DIRNAME}/somebinary #{STAGEDIR}/path/to/bin
#}

$1 $2

if [ $? != 0 ]; then
  printf 'Source control script %s failed on function %s' $0 $1
  exit 1
fi


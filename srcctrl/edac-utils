##
# Get a known-good copy of:
# edac-utils
# 

. ./functions

GITURL="https://github.com/grondo/edac-utils.git"
DIRNAME='ecac-utils'
BINARIES="\
src/util/edac-util \
src/util/edac-ctl"

fetch () { 
  git_fetch ${GITURL} ${DIRNAME}
}

unpack () {
  # nothing to do here..
  return  
}

$1 $2

if [ $? != 0 ]; then
  printf 'Source control script %s failed on function %s' $0 $1
  exit 1
fi

